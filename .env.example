# Copy to .env (gitignored) and fill in real values. docker-compose reads
# .env automatically from the project root.

# Slack Incoming Webhook URL for the prediction-drift alert
# (monitoring/grafana/provisioning/alerting/contactpoints.yaml).
# Create one at https://api.slack.com/apps -> your app -> Incoming Webhooks.
SLACK_WEBHOOK_URL=
